import 'package:flutter/material.dart';

class Particle {
  Offset position;
  double size;
  Color color;

  Particle({
    required this.position,
    required this.size,
    required this.color,
  });
}